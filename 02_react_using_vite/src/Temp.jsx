function Temp() {
    return (
        <div>
            <h2>Creating a react project using vite</h2>
            <h2>IMP!!!! :- If you get an error while running react project so edit a package.json  &quot;dev&quot;: &quot;vite --host&quot; to run the project on local host </h2>
            {/* quote is used to escape the double quotes*/}
            {/* In react we use a curly braces for comments */}
        </div>
    )
}

export default Temp